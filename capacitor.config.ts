import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neptune.converter',
  appName: 'Neptune Converter',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
